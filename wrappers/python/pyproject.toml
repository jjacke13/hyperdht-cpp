[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperdht"
version = "0.1.0"
description = "Python bindings for hyperdht-cpp via ctypes"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["hyperdht*"]
